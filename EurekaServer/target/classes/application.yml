server:
  port: 9001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
        defaultZone:
          http://localhost:${server.port}/eureka

spring:
  application:
    name: eureka-server
